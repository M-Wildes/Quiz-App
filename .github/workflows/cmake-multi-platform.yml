name: CMake (Windows & macOS)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
            qt_host: windows
            qt_arch: win64_msvc2019_64

          - os: macos-latest
            c_compiler: clang
            cpp_compiler: clang++
            qt_host: mac
            qt_arch: clang_64

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.6.1'
          host: ${{ matrix.qt_host }}
          arch: ${{ matrix.qt_arch }}
          target: desktop
          cache: true

      - name: Configure CMake
        shell: bash
        run: |
          cmake -B build \
                -S . \
                -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} \
                -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}

      - name: Build
        shell: bash
        run: cmake --build build --config Release

      # -------------------------
      # PACKAGE STEP (FIXED)
      # -------------------------
      - name: Package build
        shell: bash
        run: |
          mkdir -p dist

          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            EXE=$(find build -name "QuizApp.exe" | head -n 1)
            cp "$EXE" dist/

            # FIX: deploy Qt runtime DLLs
            windeployqt dist/QuizApp.exe
          fi

          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            APP=$(find build -name "QuizApp.app" | head -n 1)

            # IMPORTANT: preserve .app bundle structure
            cp -R "$APP" dist/

            # Optional: ensure macOS app is executable
            chmod +x dist/QuizApp.app/Contents/MacOS/*
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: QuizApp-${{ matrix.os }}
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: QuizApp Build ${{ github.run_number }}
          files: release/**/*